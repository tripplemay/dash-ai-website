# 工具包 v1 验证记录

日期：2026-09-22。环境：macOS / Python 3.9.6 / 本机 ffmpeg、ffprobe / Tabbit。

## 自动化

`python3 -m unittest discover -s tools/video-production/tests -v`：26 项通过。

- 源文件 SHA、全解码、修改后拒绝批准；传递依赖；原项目拒绝覆盖。
- 根目录逃逸、symlink 逃逸、根路径不能重绑。
- revision 冲突与非法事务回滚。
- 缺技术或维度批准阻止交付；局部通过不等于全片；局部后续否决使整片需复核。
- 新版本不继承批准；未处理反馈阻止发布；处理记录后可重新确认。
- 交付不覆盖，导出文件 SHA 与来源一致，manifest 隐去本机来源根。
- 相同活动请求去重；未知任务禁止重新 submitting；预算不足拒绝；未知费用保留；有证据的退款可更新净费用。
- 原任务 ID 不可偷换；非法费用拒绝；模拟即梦查询/下载只调用 query_result，查询失败不改任务状态。
- ffmpeg 合成素材实测：换轨保留视频包 payload/PTS/duration；半开帧范围 conform 输出帧数正确。
- HTTP Host/Origin/写入 token/路径防护；Range、suffix Range、HEAD、越界/多范围请求；素材改变拒绝流送。

Skill 格式检查通过；前端 `node --check` 通过。

## 浏览器端到端

隔离项目 `output/video-workbench/validation/projects/browser-fixture/`：

1. 创建色块视频及正弦波声音，仅用于测试。
2. 从真实浏览器写入 0.25–0.75 秒反馈并验证显示。
3. 未批准时导出被拒绝。
4. 记录处理依据；逐一记录四个维度的合成素材验收。
5. 从界面执行全解码检查；填入测试设备与合成素材依据；批准测试交付。
6. 导出新目录成功：`deliveries/release-1a3a7146d78a/`。
7. 390px 移动视口无横向溢出，无超出视口的按钮。

上述批准全部属于隔离色块测试，不是用户真实影片的主观验收。
测试中首次 `getByLabel` 精确匹配 select 标签失败，先检查现状后改用可访问 combobox role；未重复写入反馈或批准。

## 当前影片只读验证

- 导入 7 个工件，CURRENT 指针的 SHA 匹配。
- 默认版本在浏览器读到 1280×720、152.875 秒；没有 media error。
- 定位 126 秒，旧声音 v1 对照同秒定位，B 默认静音；未自动播放声音。
- 旁白语义定位实际跳至 125.411655 秒。
- 最新整片全解码及递归来源 SHA 通过；零人工批准、无正式交付。
- 原媒体及 CURRENT 指针未被本工具覆盖；无新增模型生成费用。

## 仍未验证

本次未真实提交付费任务、未测试实际即梦下载响应、未进行全片人工审听审美验收、未核验音乐授权或正式目标设备。
技术测试不能证明这些事项已经通过。
